"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const toOutDir_1 = require("./toOutDir");
const Path = require("path");
describe('toOutDir', () => {
    it('toOutDir', () => {
        const r = (0, toOutDir_1.toOutDir)(__filename);
        const isTS = /\.ts$/.test(__filename);
        if (isTS) {
            expect(r).not.toBe(__filename);
        }
        else {
            expect(r).toBe(__filename);
        }
        expect(Path.basename(r)).toBe(Path.basename(__filename));
    });
});
//# sourceMappingURL=toOutDir.test.js.map