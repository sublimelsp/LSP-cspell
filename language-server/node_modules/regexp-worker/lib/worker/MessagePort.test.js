"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const MessagePort_1 = require("./MessagePort");
describe('MessagePort', () => {
    it('nullPort', () => {
        const p = MessagePort_1.nullPort;
        p.postMessage({});
        const fn = () => { };
        expect(p.on('message', fn)).toBe(p);
        expect(p.on('close', fn)).toBe(p);
        expect(p.off('message', fn)).toBe(p);
        expect(p.off('close', fn)).toBe(p);
    });
});
//# sourceMappingURL=MessagePort.test.js.map